import React from 'react';
import Editor from '../containers/Editor';
const Note: React.FC = () => {
  return (
    <div className="note-page">
      <Editor />
    </div>
  );
};

export default Note;
